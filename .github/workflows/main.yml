name: Let me learn github actions

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print hello
      run: echo "Hello world"

    - name: Scan code
      run: echo "Scanning code..."

    - name: Build CI pipeline
      run: echo "Building pipeline..."
